TOP = $(TUP_CWD)

!babel = |> $(TOP)/node_modules/.bin/babel --module-root 'st' -M %f > %o |>
!scss = |> ^ scss %o^ sassc -I scss/ < %f > %o |>

: foreach *.es6 |> !babel |> %B.js
: foreach *.scss |> !scss |> %B.css
: foreach *.moon |> moonc %f |> %B.lua
